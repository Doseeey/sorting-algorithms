#include <iostream>
#include <fstream>
#include <string>

#define MAX_SIZE 1010295

std::string get_csv_data(std::string fileName) {
    std::string data[MAX_SIZE];

    std::fstream file(fileName, std::ios::in);
    // if(file.is_open()) {

    // }
}

int main() {
    return 0;
}